---
import SandboxLayout from '../../layouts/SandboxLayout.astro';
import BitSequenceAsciiSandbox from '../../components/sandbox/BitSequenceAsciiSandbox.svelte';
---

<SandboxLayout title="Bit Sequence: ASCII Character" description="Stage 1: 4 bits mapped to arbitrary letters (A-P). Stage 2: 7 bits mapped to standard ASCII. Click bits to flip, use the slider to scrub, or click a table cell to jump.">
  <BitSequenceAsciiSandbox client:load />
</SandboxLayout>
