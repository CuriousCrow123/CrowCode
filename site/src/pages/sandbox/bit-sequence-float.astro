---
import SandboxLayout from '../../layouts/SandboxLayout.astro';
import BitSequenceFloat from '../../components/widgets/BitSequenceFloat.svelte';
---

<SandboxLayout title="Bit Sequence: Float (IEEE 754)" description="IEEE 754 half-precision: 1 sign bit, 5 exponent bits, 10 mantissa bits. Color-coded sections show how the bit pattern encodes a decimal number. Drag-scrub increments the raw bit pattern to explore precision variation.">
  <BitSequenceFloat client:load />
</SandboxLayout>
