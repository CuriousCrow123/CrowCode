---
import SandboxLayout from '../../layouts/SandboxLayout.astro';
import BitGridBytes from '../../components/widgets/BitGridBytes.svelte';
---

<SandboxLayout title="Bit Grid: Bytes" description="Bits flip 8 at a time to foreshadow bytes and byte-addressable RAM. A CPU block sends signals along bus wires before each byte write.">
  <BitGridBytes client:load />
</SandboxLayout>
