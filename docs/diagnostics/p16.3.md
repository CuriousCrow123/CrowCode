# p16.3 — getchar Loop

## Source (with line numbers)
```c
  1 | #include <stdio.h>
  2 | int main() {
  3 |     int c;
  4 |     int count = 0;
  5 |     c = getchar();
  6 |     while (c != -1) {
  7 |         count++;
  8 |         c = getchar();
  9 |     }
 10 |     return 0;
 11 | }
```

## Instrumented Source
```c
#include "__crow.h"
#include <stdio.h>
int main() {
	__crow_push_scope("main", 2);
	__crow_step(2);
    int c;
	__crow_decl("c", &c, sizeof(c), "int", 3, 1);
	__crow_step(3);
    int count = 0;
	__crow_decl("count", &count, sizeof(count), "int", 4, 0);
	__crow_step(4);
    c = getchar();
	__crow_set("c", &c, 5);
	__crow_eval(&(c), sizeof(c), "int");
	__crow_step(5);
    while (c != -1) {
	__crow_eval_int((c != -1));
	__crow_step_col(6, 10, 19);
        count++;
	__crow_set("count", &count, 7);
	__crow_eval_int(count);
	__crow_step(7);
        c = getchar();
	__crow_set("c", &c, 8);
	__crow_eval(&(c), sizeof(c), "int");
	__crow_step(8);
    }
    __crow_step(10);
	__crow_pop_scope();
	return 0;
}
```

## Steps (3 total)

### Step 0 | Line 2 — Enter main() | 3 ops
- addEntry(parent=null, id=stdin, name=stdin, type=char[], val=ABC, kind=io, addr=, children=0)
- addEntry(parent=null, id=main, name=main, type=, val=, kind=scope, addr=, children=0)
- addEntry(parent=null, id=heap, name=Heap, type=, val=, kind=heap, addr=, children=0)

### Step 1 | Line 3 — Declare int c = ? (uninitialized) | 1 ops
- addEntry(parent=main, id=main::c, name=c, type=int, val=?, kind=, addr=0x00001ff0, children=0)

### Step 2 | Line 4 — Declare int count = 0 | 1 ops
- addEntry(parent=main, id=main::count, name=count, type=int, val=0, kind=, addr=0x00001ff4, children=0)

## Snapshots (3 total)

### Snapshot 0 (after step 0)
- stdin | name=stdin | type=char[] | val=ABC | addr= | kind=io | heap=
- main | name=main | type= | val= | addr= | kind=scope | heap=
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=

### Snapshot 1 (after step 1)
- stdin | name=stdin | type=char[] | val=ABC | addr= | kind=io | heap=
- main | name=main | type= | val= | addr= | kind=scope | heap=
  - main::c | name=c | type=int | val=? | addr=0x00001ff0 | kind= | heap=
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=

### Snapshot 2 (after step 2)
- stdin | name=stdin | type=char[] | val=ABC | addr= | kind=io | heap=
- main | name=main | type= | val= | addr= | kind=scope | heap=
  - main::c | name=c | type=int | val=? | addr=0x00001ff0 | kind= | heap=
  - main::count | name=count | type=int | val=0 | addr=0x00001ff4 | kind= | heap=
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=
