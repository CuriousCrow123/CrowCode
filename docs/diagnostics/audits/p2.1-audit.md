# p2.1 — Simple Struct — Audit

## Line-by-Line

| Source Line | C Statement | Expected Step? | Actual Step | Expected Ops | Actual Ops | Expected Values | Actual Values | Status |
|---|---|---|---|---|---|---|---|---|
| 2 | `int main() {` | Step 0, Line 2 | Step 0, Line 2 | addEntry(main scope), addEntry(heap) | addEntry(main), addEntry(heap) | -- | -- | OK |
| 3 | `struct Point p = {10, 20};` | Step 1, Line 3 | Step 1, Line 3 | addEntry(p, struct Point, val shows {10,20}) | addEntry(main::p, struct Point, val=) | p.x=10, p.y=20 visible | val= (empty) | BUG: no struct field children or values shown |
| 4 | `p.x = 30;` | Step 2, Line 4 | Step 2, Line 4 | setValue(p, shows fields x=30,y=20) | setValue(main::p, val=) | p.x=30, p.y=20 | val= (empty) | BUG: struct value not shown |
| 5 | `p.y = p.x + 5;` | Step 3, Line 5 | Step 3, Line 5 | setValue(p, shows x=30,y=35) | setValue(main::p, val=) | p.x=30, p.y=35 | val= (empty) | BUG: struct value not shown |
| 6 | `return 0;` | Step 4, Line 6 | Step 4, Line 5 | removeEntry(main) | removeEntry(main) | -- | -- | BUG: line should be 6, actual is 5 |

## Bugs Found
- [ ] BUG-p2.1-1: Struct `p` is declared with no children and empty val. Struct fields (x, y) are never shown as child entries or in the value string. The user cannot see struct member values at any step.
- [ ] BUG-p2.1-2: setValue ops for struct `p` always show val= (empty). After `p.x = 30`, the snapshot should show x=30 somewhere but it does not.
- [ ] BUG-p2.1-3: The cleanup step (Step 4) reports Line 5 but should report Line 6 (`return 0`) or at minimum the closing brace line 7. The `__crow_step` before return is never reached because `__crow_pop_scope()` precedes `return 0` and the step for return is absent from instrumented code.
