# p2.1 — Simple Struct

## Source (with line numbers)
```c
  1 | struct Point { int x; int y; };
  2 | int main() {
  3 |     struct Point p = {10, 20};
  4 |     p.x = 30;
  5 |     p.y = p.x + 5;
  6 |     return 0;
  7 | }
```

## Instrumented Source
```c
#include "__crow.h"
struct Point { int x; int y; };
int main() {
	__crow_push_scope("main", 2);
	__crow_step(2);
    struct Point p = {10, 20};
	__crow_decl("p", &p, sizeof(p), "struct Point", 3, 0);
	__crow_step(3);
    p.x = 30;
	__crow_set("p", &p, 4);
	__crow_eval(&(p.x), sizeof(p.x), "int");
	__crow_step(4);
    p.y = p.x + 5;
	__crow_set("p", &p, 5);
	__crow_eval(&(p.y), sizeof(p.y), "int");
	__crow_step(5);
    __crow_step(6);
	__crow_pop_scope();
	return 0;
}
```

## Steps (6 total)

### Step 0 | Line 2 — Enter main() | 2 ops
- addEntry(parent=null, id=main, name=main, type=, val=, kind=scope, addr=, children=0)
- addEntry(parent=null, id=heap, name=Heap, type=, val=, kind=heap, addr=, children=0)

### Step 1 | Line 3 — Declare struct Point p = {...} | 1 ops
- addEntry(parent=main, id=main::p, name=p, type=struct Point, val=, kind=, addr=0x00001ff0, children=2)

### Step 2 | Line 4 — Set p.x = 30 → 30 | 3 ops
- setValue(id=main::p, val=)
- setValue(id=main::p.x, val=30)
- setValue(id=main::p.y, val=20)

### Step 3 | Line 5 — Set p.y = p.x + 5 → 35 | 3 ops
- setValue(id=main::p, val=)
- setValue(id=main::p.x, val=30)
- setValue(id=main::p.y, val=35)

### Step 4 | Line 6 — return 0 | 0 ops

### Step 5 | Line 6 — return 0 | 1 ops
- removeEntry(id=main)

## Snapshots (6 total)

### Snapshot 0 (after step 0)
- main | name=main | type= | val= | addr= | kind=scope | heap=
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=

### Snapshot 1 (after step 1)
- main | name=main | type= | val= | addr= | kind=scope | heap=
  - main::p | name=p | type=struct Point | val= | addr=0x00001ff0 | kind= | heap=
    - main::p.x | name=x | type=int | val=10 | addr=0x00001ff0 | kind= | heap=
    - main::p.y | name=y | type=int | val=20 | addr=0x00001ff4 | kind= | heap=
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=

### Snapshot 2 (after step 2)
- main | name=main | type= | val= | addr= | kind=scope | heap=
  - main::p | name=p | type=struct Point | val= | addr=0x00001ff0 | kind= | heap=
    - main::p.x | name=x | type=int | val=30 | addr=0x00001ff0 | kind= | heap=
    - main::p.y | name=y | type=int | val=20 | addr=0x00001ff4 | kind= | heap=
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=

### Snapshot 3 (after step 3)
- main | name=main | type= | val= | addr= | kind=scope | heap=
  - main::p | name=p | type=struct Point | val= | addr=0x00001ff0 | kind= | heap=
    - main::p.x | name=x | type=int | val=30 | addr=0x00001ff0 | kind= | heap=
    - main::p.y | name=y | type=int | val=35 | addr=0x00001ff4 | kind= | heap=
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=

### Snapshot 4 (after step 4)
- main | name=main | type= | val= | addr= | kind=scope | heap=
  - main::p | name=p | type=struct Point | val= | addr=0x00001ff0 | kind= | heap=
    - main::p.x | name=x | type=int | val=30 | addr=0x00001ff0 | kind= | heap=
    - main::p.y | name=y | type=int | val=35 | addr=0x00001ff4 | kind= | heap=
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=

### Snapshot 5 (after step 5)
- heap | name=Heap | type= | val= | addr= | kind=heap | heap=
